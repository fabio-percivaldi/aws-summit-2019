import React from 'react';

const Header = props => {
    return (
        <nav className="navbar-dark fixed-top bg-dark flex-md-nowrap p-0 shadow" style={{'zIndex': '100000', 'height': '50px'}}>
            <div className="container-fluid">
                <div className="row">
                </div>
            </div>  
        </nav>
    );
}

export default Header;
